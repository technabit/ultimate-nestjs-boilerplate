import * as React from 'react';
interface EmailVerificationProps {
    email: string;
    url: string;
}
export declare const EmailVerification: ({ email, url, }: EmailVerificationProps) => React.JSX.Element;
export default EmailVerification;
