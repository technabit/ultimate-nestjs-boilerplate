import * as React from 'react';
interface ResetPasswordProps {
    email: string;
    url: string;
}
export declare const ResetPassword: ({ email, url, }: ResetPasswordProps) => React.JSX.Element;
export default ResetPassword;
