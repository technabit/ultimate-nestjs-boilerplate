generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Temp {
  id                   String   @id @db.Uuid
  title                String

  @@map("temp")
}
